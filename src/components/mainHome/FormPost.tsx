"use client"
import { useForm } from "react-hook-form"

export const FormPost = ({sessionImage}: {sessionImage: string}) => {
  const { register, handleSubmit } = useForm()

  return <div className="w-full shadow-md bg-white rounded-md p-4 mt-5">
    
  </div>
}